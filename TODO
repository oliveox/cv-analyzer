Requirement:
- Python Flask application that presents your CV data
- CV data can be hard-coded in the code or read from disk. You do not need to integrate with any database.

TODO:
- Flask CLI command that prints the data to the console
    - add description, details on cli command
    - constrains on cli argument
- README file on how to start the REST API and how to execute the CLI command

Done:
- JSON REST API with endpoints GET /personal, GET /experience, GET /education

Optional:
- POST endpoint for CV upload (always keep the last updated)
- 404 for not implemented API's
- prettify cli json result